# Http Server (stdlib only + sql driver) in Go
 - lets see what I got
 - plan is to use only stdlib and a sql driver to create a CRUD REST API for a USER/TODO db


# Easiest way to run 
 - make run

# Makefile
 - make run
 - make build